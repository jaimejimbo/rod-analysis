Library to create videos for monolayer vibrated rod system.

This processes data on a Map-Reduce way (with multiprocessing).

Dependencies: sqlite3, numpy, matplotlib, re, multiprocessing

- Use imagej to create rod data file.
- Create database with create_db.py.
- Insert values in database(previously created) with insert_db.py, dates_db.py.
- Launch modules to create videos (order_param.py, correlation.py, ...).
